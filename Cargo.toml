[package]
name = "smoljail"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0"
clap = { version = "4.6.1", features = ["derive"] }
landlock = "0.4.4"
libc = "0.2"
nix = { version = "0.31", features = ["fs", "user", "process", "signal", "mount", "sched"] }
